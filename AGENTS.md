# 저장소 작업 지침

이 저장소에서 파일을 변경하는 모든 에이전트는 다음 절차를 따른다.

1. 작업 시작 전에 `project-state.json`, `docs/STATUS.md`, 관련 설계 문서를 읽고 깨끗한 Git 트리에서 다음 명령을 실행한다.

```text
python tools/task_harness.py begin
```

기존 변경이 있으면 하네스가 시작을 거부한다. 해당 변경의 소유권을 확인하기 전에는 자동 게시하지 않는다.

2. 구현·자산·문서 중 하나를 바꾸면 대응되는 나머지 항목도 같은 작업에서 갱신한다.
3. 현재 사실은 `project-state.json`에 한 번만 기록한다. `docs/STATUS.md`는 하네스로 생성하며 직접 편집하지 않는다.
4. 후보값을 승인값처럼 쓰지 않는다. `*.candidate.json`은 검수 전이며 최종 `brand.json`과 구분한다.
5. 예외 처리된 레퍼런스를 스타일 학습이나 기준 이미지에 다시 넣지 않는다.
6. 불필요하거나 폐기된 문서·자산·참조는 같은 변경에서 삭제한다.
7. 파일 변경 작업을 마칠 때 다음을 실행한다.

```text
python tools/task_harness.py finish --message "한글 커밋 메시지"
```

하네스는 상태 문서 생성, JSON·매니페스트·참조 검증, 커밋과 현재 브랜치 푸시를 순서대로 실행한다. 자동 게시를 원하지 않는 작업은 `check`만 실행한다.

```text
python tools/task_harness.py check
```

Git 작업 트리가 작업 시작 전부터 변경된 상태라면 자동 커밋하지 말고 변경 소유권을 먼저 확인한다.
