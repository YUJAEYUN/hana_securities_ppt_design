# 1. 스켈레톤과 입력 자료 준비

## 목적

구현 전에 파일 책임, 함수 경계, 입력 자료 위치를 고정한다. 이 단계에서는 완성 로직을 넣지 않고 실행 가능한 최소 스켈레톤과 테스트 자리를 만든다.

## 작업

1. `skill-creator`의 초기화 스크립트로 `hana-ppt-skill`을 생성한다.
2. `scripts`, `references`, `assets`를 포함하고 필요한 `agents/openai.yaml`을 생성한다.
3. 원문 요구에 따라 `AGENTS.md`, `requirements.txt`, `examples`, `tests`를 추가한다.
4. `assets/fonts/windows/`와 `assets/fonts/macos/`를 만들고 CI/하나서체 원본을 OS별로 배치한다.
5. 각 Python 파일에 CLI 진입점, 함수 시그니처, 타입 힌트, docstring을 작성한다.
6. 샘플과 브랜드 자산을 저장할 위치를 정하되, 민감하거나 대용량인 원본은 Git 추적 여부를 확인한다.

## 권장 함수 경계

### `extract_tokens.py`

```python
def analyze_presentation(path): ...
def extract_colors(presentation): ...
def extract_typography(presentation): ...
def extract_geometry(presentation): ...
def detect_repeated_images(presentations): ...
def detect_page_numbers(presentation): ...
def classify_slide(slide, features): ...
def aggregate_tokens(analyses): ...
def write_brand_json(tokens, output_path): ...
def write_design_tokens(tokens, output_path): ...
def main(): ...
```

### `build_deck.py`

```python
def load_brand(path): ...
def build_cover(prs, content, brand_path): ...
def build_toc_slide(prs, content, brand_path): ...
def build_bullet_slide(prs, content, brand_path): ...
def build_comparison_slide(prs, content, brand_path): ...
def build_closing_slide(prs, content, brand_path): ...
def main(): ...
```

각 `build_*` 함수는 `brand_path`를 받아 호출 시점에 파일을 다시 읽는다.

### `ingest_deck.py`와 `restyle_deck.py`

```python
def extract_deck_spec(input_path): ...
def inventory_text_tables_charts_media(presentation): ...
def select_workflow_mode(requested_mode): ...
def build_change_plan(deck_spec, mode): ...
def map_slide_to_layout(slide_spec, layouts): ...
def check_content_lock(source, output): ...
def main(): ...
```

`restyle-only`에서는 문장, 수치와 표·차트 데이터를 잠근다. 디자인 개선을 위해 페이지 수나 레이아웃이 달라질 수 있으며 원본과 결과의 슬라이드 대응표를 남긴다. `hana-refine`은 초안의 사실과 근거를 유지하고 승인된 말투·구조 변경 계획과 결과를 함께 보존한다.

### `quality_check.py`

```python
def check_colors(presentation, brand): ...
def check_fonts(presentation, brand): ...
def check_logo_geometry(presentation, brand): ...
def estimate_text_overflow(shape, brand): ...
def build_report(results): ...
def main(): ...
```

### `font_preflight.py`

```python
def detect_platform(): ...
def load_font_manifest(brand_path): ...
def validate_bundled_font_files(fonts, skill_root): ...
def list_installed_font_families(platform_name): ...
def check_required_font_families(required, installed): ...
def print_install_guidance(platform_name, missing, fonts): ...
def main(): ...
```

사전 검사는 읽기 전용으로 동작한다. 폰트 형식, 패밀리명, 스타일, 임베딩 권한, 설치 여부를 구분해 보고한다. 폰트 파일을 자동 설치하거나 시스템 폰트 디렉터리를 변경하지 않는다.

### `embed_fonts.py`

```python
def inspect_embedding_rights(font_path): ...
def choose_delivery_strategy(fonts, environment): ...
def embed_bundled_fonts(input_pptx, output_pptx, fonts): ...
def save_with_powerpoint_embedding(input_pptx, output_pptx): ...
def verify_embedded_fonts(output_pptx, expected_families): ...
def main(): ...
```

임베딩 구현은 결과를 PowerPoint에서 다시 열어 실제 임베딩 여부를 확인하는 통합 테스트를 통과해야 한다. 지원되지 않는 환경에서 성공한 것처럼 처리하지 않는다.

### `render_slides.py`

```python
def detect_available_renderers(): ...
def render_with_powerpoint(input_pptx, output_dir): ...
def render_with_libreoffice(input_pptx, output_dir): ...
def convert_pdf_pages_to_png(pdf_path, output_dir): ...
def write_render_manifest(renderer, images, output_path): ...
def main(): ...
```

Windows에서는 데스크톱 PowerPoint 렌더링을 최종 기준으로 삼는다. PowerPoint가 없으면 LibreOffice headless를 사용하되 결과를 대체 렌더링으로 표시한다. macOS PowerPoint 자동화는 구현 시 실제 지원 범위를 검증하고, 검증되지 않으면 LibreOffice 또는 수동 내보내기 경로를 사용한다.

### `visual_check.py`와 `build_contact_sheet.py`

```python
def detect_content_bounds(image): ...
def detect_edge_clipping(image): ...
def score_whitespace_and_density(image): ...
def compare_alignment_across_slides(images): ...
def compare_reference_metrics(image, reference_set): ...
def build_contact_sheet(images, labels, output_path): ...
def write_visual_report(results, output_path): ...
def main(): ...
```

`visual_check.py`는 결정적인 이미지 지표만 계산하고 특정 AI API를 호출하지 않는다. 에이전트는 contact sheet와 개별 슬라이드 PNG를 현재 환경의 이미지 이해 기능으로 검토한다.

## 의존성 초안

- `python-pptx`: PPTX 읽기와 생성
- `Pillow`: 이미지 크기와 포맷 보조 분석
- `fonttools`: 폰트 메타데이터, 임베딩 권한, 글리프 검사
- `PyMuPDF`: PDF 페이지를 PNG로 변환
- `opencv-python-headless`: 경계, 정렬, 겹침과 밀도 이미지 분석
- `pytest`: 자동 테스트
- 필요 시 `lxml`: OOXML 수준의 제한적 검사
- Windows PowerPoint 자동화가 필요할 때만 `pywin32`를 선택 의존성으로 사용

버전은 구현 시 현재 호환성을 확인해 고정한다. 각 스크립트는 누락된 필수 패키지를 이해하기 쉬운 메시지로 안내해야 한다.

## 입력 자료 체크리스트

- [ ] PPTX 5개 이상
- [ ] 각 PPTX의 최신성 또는 공식성 표시
- [ ] 최소 5개 목표 슬라이드 유형을 포함하는 사례
- [ ] CI와 하나서체의 Windows용 파일을 `assets/fonts/windows/`에 배치
- [ ] CI와 하나서체의 macOS용 파일을 `assets/fonts/macos/`에 배치
- [ ] 각 파일의 정확한 폰트 패밀리명·스타일·확장자 확인
- [ ] 공식 로고 SVG/PNG
- [ ] 기본 화면비 확인(예: 16:9)
- [ ] 페이지 번호 포함 여부와 예외 슬라이드 확인

## 완료 조건

- 모든 필수 디렉터리와 파일이 존재한다.
- Python 파일이 문법 오류 없이 `--help`를 출력한다.
- 테스트 러너가 빈 테스트 구조에서 정상 시작한다.
- `font_preflight.py`가 OS별 자산 누락과 미설치 폰트를 구분해 보고한다.
- 첨부 PPTX에서 텍스트·표·차트·이미지 인벤토리를 추출한다.
- 외부 초안 인수 계약과 두 후처리 워크플로의 변경 허용 범위가 자동 테스트로 고정된다.
- 렌더러가 있는 환경에서 샘플 슬라이드 PNG와 렌더 manifest를 생성한다.
- 고의로 잘림·겹침·정렬 이탈을 넣은 렌더 픽스처를 시각 검사에서 검출한다.
- 누락된 입력 자료가 목록으로 명확히 보고된다.
